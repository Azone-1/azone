# 📝 AZone Project - မှတ်စု

> ဒီ file က ငါ့အတွက် personal note ပါ။ အားတဲ့အချိန်မှာ ဖတ်ဖို့ ရေးထားတာ။

---

## ✅ မင်း (AI) လုပ်ပြီးသွားတာတွေ

### 1. API Templates ဖန်တီးပြီး
- `api_templates.py` file ထဲမှာ
- Leads, Customers, Projects, Dashboard APIs
- Error handling built-in
- Role-based access control

### 2. Frontend Helpers ဖန်တီးပြီး
- `static/js/api-helpers.js` file ထဲမှာ
- API request functions
- UI helper functions
- Form validation helpers

### 3. UI Improvements ထည့်ပြီး
- `static/css/ui-improvements.css` - Loading, alerts, forms
- `static/js/toast-notifications.js` - Toast system

### 4. Documentation ရေးပြီး
- Usage guides
- Examples
- Checklists

---

## 📝 ငါ လုပ်ရမဲ့အရာတွေ

### Phase 1: Database Setup

#### 1. Database Schema Design
ဘာ tables လိုမလဲ ဆုံးဖြတ်ရမယ်:

**Leads Table:**
- id (PRIMARY KEY)
- name (TEXT)
- email (TEXT)
- phone (TEXT)
- service (TEXT)
- status (TEXT) - 'New', 'Contacted', 'Converted'
- created_at (TIMESTAMP)
- created_by (INTEGER) - user id

**Customers Table:**
- id (PRIMARY KEY)
- name (TEXT)
- email (TEXT)
- phone (TEXT)
- package (TEXT)
- status (TEXT) - 'Active', 'Trial', 'Suspended', 'Inactive'
- join_date (TIMESTAMP)

**Projects Table:**
- id (PRIMARY KEY)
- name (TEXT)
- customer_id (INTEGER) - FOREIGN KEY
- service (TEXT)
- status (TEXT) - 'todo', 'in-progress', 'in-review', 'completed'
- priority (TEXT) - 'High', 'Medium', 'Low'
- created_at (TIMESTAMP)

#### 2. Database Functions Implement
`db_manager.py` ထဲမှာ functions ရေးရမယ်:

```python
# Example functions needed:
def init_leads_table()
def get_leads(search='', service='', status='', page=1, per_page=50)
def create_lead(name, email, phone, service, status='New', created_by=None)
def update_lead(lead_id, **data)
def delete_lead(lead_id)

def init_customers_table()
def get_customers(search='', status='', package='', page=1, per_page=50)
def create_customer(name, email, phone, package, status='Active')
def update_customer(customer_id, **data)
def delete_customer(customer_id)

def init_projects_table()
def get_projects(search='', status='', service='')
def create_project(name, customer_id, service, status='todo', priority='Medium')
def update_project_status(project_id, status)

def get_dashboard_stats()
def get_chart_data(period='30days')
```

---

### Phase 2: Backend Connection

#### 1. API Templates ကို Database နဲ့ Connect
`api_templates.py` ထဲမှာ `TODO` comments တွေကို fill in:

```python
# Example: get_leads_template() function ထဲမှာ

# Replace this:
leads = []

# With this:
import db_manager
leads = db_manager.get_leads(
    search=search,
    service=service_filter,
    status=status_filter,
    page=page,
    per_page=per_page
)
```

#### 2. Routes Register လုပ်ပါ
`web_app.py` ထဲမှာ:

```python
from api_templates import (
    get_leads_template,
    create_lead_template,
    update_lead_template,
    delete_lead_template,
    get_customers_template,
    create_customer_template,
    get_projects_template,
    create_project_template,
    update_project_status_template,
    get_dashboard_stats_template,
    get_dashboard_chart_data_template
)

# Register routes
get_leads_template()
create_lead_template()
update_lead_template()
delete_lead_template()
get_customers_template()
create_customer_template()
get_projects_template()
create_project_template()
update_project_status_template()
get_dashboard_stats_template()
get_dashboard_chart_data_template()
```

---

### Phase 3: Frontend Connection

#### 1. HTML Templates ထဲမှာ Include လုပ်ပါ
`templates/index.html` သို့မဟုတ် `templates/dashboard.html`:

```html
<!-- CSS -->
<link rel="stylesheet" href="{{ url_for('static', filename='css/ui-improvements.css') }}">

<!-- JavaScript -->
<script src="{{ url_for('static', filename='js/api-helpers.js') }}"></script>
<script src="{{ url_for('static', filename='js/toast-notifications.js') }}"></script>
```

#### 2. JavaScript Functions သုံးပါ
HTML ထဲမှာ script tag add လုပ်ပြီး:

```javascript
// Load leads table
async function loadLeadsTable() {
    showLoading('leads-table-body');
    
    const result = await getLeads({
        search: document.getElementById('lead-search').value,
        service: document.getElementById('filter-service').value,
        status: document.getElementById('filter-status').value
    });
    
    if (result.success) {
        displayTableData(result.data, 'leads-table-body', (lead) => {
            return `<tr>...</tr>`;
        });
        showSuccessToast('Loaded successfully!');
    } else {
        showErrorToast(result.error);
    }
    
    hideLoading('leads-table-body');
}

// Page load မှာ call
document.addEventListener('DOMContentLoaded', () => {
    loadLeadsTable();
});
```

---

## 🎯 Step-by-Step Plan

### Step 1: Database (အရင်)
1. Database schema design
2. Tables create လုပ်ပါ
3. Database functions implement လုပ်ပါ
4. Test database functions

### Step 2: Backend (နောက်)
1. `api_templates.py` ထဲမှာ TODO fill in
2. Routes register လုပ်ပါ
3. Test APIs (Postman သို့မဟုတ် browser)

### Step 3: Frontend (နောက်ဆုံး)
1. HTML templates ထဲမှာ include လုပ်ပါ
2. JavaScript functions add လုပ်ပါ
3. Event listeners setup လုပ်ပါ
4. Test everything

---

## 📚 Files တွေ ဘယ်မှာ ရှိလဲ

### Backend:
- `api_templates.py` - API route templates
- `db_manager.py` - Database functions (သင်ရေးရမယ်)

### Frontend:
- `static/js/api-helpers.js` - API helper functions
- `static/js/toast-notifications.js` - Toast system
- `static/css/ui-improvements.css` - UI styles

### Documentation:
- `example_usage.html` - Working example
- `အသုံးပြုပုံ_လမ်းညွှန်.md` - Detailed guide

---

## 💡 Tips

1. **Start Small**: Leads API ကို အရင် implement လုပ်ပါ
2. **Test Frequently**: တစ်ခုပြီးတိုင်း test လုပ်ပါ
3. **Use Examples**: `example_usage.html` ကို reference လုပ်ပါ
4. **Read Documentation**: `အသုံးပြုပုံ_လမ်းညွှန်.md` ကို ဖတ်ပါ

---

## 🆘 Troubleshooting

### API response မရဘူး
- Browser console check လုပ်ပါ
- Network tab check လုပ်ပါ
- Backend logs check လုပ်ပါ

### Database error
- Database file path သေချာပါစေ
- Tables create လုပ်ထားတာ သေချာပါစေ
- Functions test လုပ်ပါ

### Frontend မအလုပ်လုပ်ဘူး
- JavaScript files include လုပ်ထားတာ သေချာပါစေ
- Console errors check လုပ်ပါ
- API endpoints test လုပ်ပါ

---

## ✅ Quick Checklist

### Database:
- [ ] Schema design
- [ ] Tables create
- [ ] Functions implement
- [ ] Test functions

### Backend:
- [ ] TODO fill in
- [ ] Routes register
- [ ] Test APIs

### Frontend:
- [ ] Files include
- [ ] Functions add
- [ ] Event listeners
- [ ] Test everything

---

## 🎯 နောက်ဆုံး

အကုန် ready ဖြစ်ပြီ! အားတဲ့အချိန်မှာ:
1. Database setup လုပ်ပါ
2. Backend connect လုပ်ပါ
3. Frontend connect လုပ်ပါ
4. Test လုပ်ပါ

**အားတဲ့အချိန်မှာ ဒီ note ကို ဖတ်ပြီး စလုပ်နိုင်ပါတယ်!** 🚀

---

## 📝 Personal Notes

- ဒီ project က bot builder + CRM system ဖြစ်တယ်
- Railway.app မှာ deploy လုပ်မယ်
- Domain: paing.xyz
- GitHub: https://github.com/Apaing9611-arch/azone.git

**အားတဲ့အချိန်မှာ ဒီ note ကို ဖတ်ပြီး continue လုပ်နိုင်ပါတယ်!** ✨
